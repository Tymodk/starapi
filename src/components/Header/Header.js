import * as React from 'react';


const Header = (props) => (
    <header>
        <div className="page-header">
            <h1 className="starHeader">STAR WARS <small className="starSmall"> API </small></h1>
        </div>

    </header>

        

);

export default Header;